{% layout none %}

{% liquid
  assign view_type = request.page_type
  assign design_mode = request.design_mode
  assign host = request.host
  assign locale = request.locale
  assign origin = request.origin
  assign path = request.path
%}

{
  "name": "Custom Product Template JSON",
  "settings": [],
  "blocks": [],
  "presets": [
    {
      "name": "Custom Product Template JSON",
      "category": "Product"
    }
  ],
  "view_type": "{{ view_type }}",
  "design_mode": "{{ design_mode }}",
  "host": "{{ host }}",
  "locale": {{ locale | json }},
  "origin": "{{ origin }}",
  "path": "{{ path }}"
}